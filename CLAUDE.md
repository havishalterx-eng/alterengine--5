# Alter Engine

**All agent rules live in [`AGENTS.md`](AGENTS.md). Read it before doing anything.**

It is the single canonical rules file for every agent on this project, regardless of tool. Nothing is duplicated here — if the rules existed in two places they would drift, which is exactly the class of defect this build is designed to prevent.

Start there.
